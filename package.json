{
  "name": "mealplanner",
  "version": "1.0.0",
  "description": "Personal recipe, meal planning, and pantry tracker — deploys to Netlify",
  "scripts": {
    "dev": "netlify dev",
    "local": "node server.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
